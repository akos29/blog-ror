  <% @users.each do |user| %>
    <div class= 'user_profile'>
      <img src=<%=(user.photo) %> />
      <h2><%= user.name %></h2> 
      <p> Number of Posts: <span class ='rev'><%= user.posts_count %></span></p>
    </div> 
  <%end%>
