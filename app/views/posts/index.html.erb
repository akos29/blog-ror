<h1>Posts List</h1>
  <%= render "shared/user", user: @user %>
  <% @posts.each do |post| %>
  <%# <%= user.photo%>
    
    <div>
      <h2><%= post.title %></h2> 
      <%= post.text %>
    </div>
    <div>
      Likes: #<%= post.likes_count %>
      Comments: #<%= post.comments_count %>
    </div> 
    <%# link_to  %>
  <% end %>